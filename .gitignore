node_modules/
dist/
.yarn/
.pnp.*
yarn-error.log
coverage/
*.log
.turbo/
.DS_Store
.mercato/
.env
.env.local

# wrangler local state. `wrangler ai-search` writes an account identity file
# (account ID plus the account's display name, usually an email address) on
# first login — noise and a disclosure risk both.
.wrangler/

# OMC tooling session state and internal planning docs — kept locally, not in
# the public package repo.
.omc/
HANDOFF.md
